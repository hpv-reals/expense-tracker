# Xcode / iOS
#
# This project's .xcodeproj is generated by `xcodegen` from project.yml,
# so it is not committed — run `xcodegen generate` after cloning to recreate it.
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.moved-aside
*.pbxuser
*.perspectivev3
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

DerivedData/
build/
.build/

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
Package.resolved

# CocoaPods / Carthage (unused here, but standard to exclude)
Pods/
Carthage/Build/

# fastlane (unused here, but standard to exclude)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Obj-C/Swift specific
*.hmap

# Playgrounds
timeline.xctimeline
playground.xcworkspace

# macOS
.DS_Store

# Claude Code local session state
.claude/
